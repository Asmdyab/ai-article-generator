# 📝 شرح كامل لمشروع AI Article Agent

---

## 📌 نظرة عامة

هذا تطبيق ويب **Agent-based** لتوليد المقالات باستخدام الذكاء الاصطناعي، مبني بـ **Next.js 16** مع **TypeScript** و **AI SDK 6**.

### 🤖 ما هو الـ Agent?
الـ Agent هو نموذج لغوي كبير (LLM) يستخدم أدوات (Tools) في حلقة لإنجاز المهام. الفرق بينه وبين الـ Flow العادي:
- **Flow**: تحدد أنت تسلسل الخطوات مسبقاً
- **Agent**: الموديل يقرر أي أدوات يستخدم ومتى وبأي ترتيب

### التقنيات المستخدمة:
- **Google Vertex AI (Gemini 2.5 Flash)** - الموديل الأساسي للـ Agent
- **Imagen 3** - لتوليد الصور
- **Exa** - للبحث على الإنترنت
- **AI SDK 6** - لإنشاء الـ Agent مع Tools

---

## 📁 هيكل المشروع (Structure)

```
ai-article-generator/
├── app/                          # مجلد التطبيق الرئيسي (Next.js App Router)
│   ├── api/
│   │   ل── agent/route.ts        # 🤖 الـ Agent الرئيسي مع Tools
│   ├── globals.css               # التنسيقات العامة
│   ├── layout.tsx                # القالب الرئيسي
│   ل── page.tsx                  # الصفحة الرئيسية
├── components/                   # المكونات
│   ├── AnimatedBackground.tsx    # الخلفية المتحركة
│   ├── ArticlePanel.tsx          # لوحة عرض المقال
│   ├── ChatMessage.tsx           # رسالة الشات
│   ├── ChatPanel.tsx             # لوحة الشات
│   ل── Loader.tsx                # مؤشر التحميل
├── lib/
│   ل── utils.ts                  # دوال مساعدة
ل── package.json                  # الاعتماديات
```

---

## 📦 ملف package.json - الاعتماديات

```json
"dependencies": {
  "@ai-sdk/google-vertex": "^4.0.28",  // SDK للتواصل مع Vertex AI
  "@google-cloud/vertexai": "^1.10.0",  // مكتبة Google Cloud
  "ai": "^6.0.48",                       // Vercel AI SDK
  "exa-js": "^2.1.1",                    // مكتبة Exa للبحث
  "google-auth-library": "^10.5.0",      // للمصادقة مع Google
  "lucide-react": "^0.563.0",            // أيقونات
  "next": "16.1.4",                      // إطار العمل
  "react": "19.2.3",                     // مكتبة React
  "tailwind-merge": "^3.4.0",            // دمج classes
  "zod": "^3.25.76"                      // Schema Validation
}
```

---

## 🏠 الصفحة الرئيسية - app/page.tsx

### الـ Interfaces (أنواع البيانات)

```typescript
// نقطة في المقال
interface ArticlePoint {
  heading: string;         // عنوان النقطة
  content: string;         // المحتوى
  imagePrompt: string;     // وصف الصورة للتوليد
  shouldHaveImage: boolean;// هل تحتاج صورة؟
  imageData?: string;      // بيانات الصورة (Base64)
}

// المقال الكامل
interface Article {
  title: string;           // العنوان
  introduction: string;    // المقدمة
  points: ArticlePoint[];  // النقاط
  conclusion: string;      // الخاتمة
}

// رسالة الشات
interface Message {
  id: string;              // معرف فريد
  role: 'user' | 'assistant'; // المرسل
  content: string;         // المحتوى
}
```

### الـ State Variables (متغيرات الحالة)

```typescript
const [input, setInput] = useState('');           // نص الإدخال
const [messages, setMessages] = useState<Message[]>([]); // رسائل الشات
const [isLoading, setIsLoading] = useState(false); // حالة التحميل
const [status, setStatus] = useState('');          // رسالة الحالة
const [currentArticle, setCurrentArticle] = useState<Article | null>(null); // المقال
```

### دالة handleSend - إرسال الرسالة

```typescript
const handleSend = async () => {
  // 1. التحقق من وجود نص
  if (!input.trim() || isLoading) return;

  // 2. إنشاء رسالة المستخدم بمعرف فريد
  const userMessage: Message = {
    id: `user-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`,
    role: 'user',
    content: input.trim(),
  };

  // 3. تحديث الحالة
  setMessages(prev => [...prev, userMessage]);
  setInput('');
  setIsLoading(true);

  // 4. إرسال للـ Agent API
  const response = await fetch('/api/agent', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ message: userMessage.content }),
  });

  // 5. قراءة الـ Stream (البيانات المتدفقة)
  const reader = response.body?.getReader();
  const decoder = new TextDecoder();
  
  // 6. معالجة كل جزء من البيانات
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    
    const chunk = decoder.decode(value);
    // معالجة الأنواع المختلفة: status, chat, article, image, done, error
  }
};
```

### أنواع الأحداث من الـ Stream

| النوع | الوظيفة |
|-------|---------|
| `status` | تحديث حالة العملية |
| `chat` | رسالة من الـ AI |
| `article` | بيانات المقال الكامل |
| `image` | صورة لنقطة معينة |
| `done` | انتهاء العملية |
| `error` | حدث خطأ |

---

## 🤖 الـ Agent API - app/api/agent/route.ts

### الفرق بين Flow و Agent

**الطريقة القديمة (Flow):**
```
رسالة المستخدم → قرار الأداة → بحث → مقال → صور (تسلسل ثابت)
```

**الطريقة الجديدة (Agent):**
```
رسالة المستخدم → الموديل يقرر أي أداة يستخدم → ينفذها → يقرر الخطوة التالية (حلقة ذكية)
```

### الأدوات (Tools) المتاحة للـ Agent

```typescript
// Tool 1: البحث على الإنترنت
search_web: tool({
  description: 'Search the web for information about a topic',
  inputSchema: z.object({
    query: z.string().describe('The search query'),
  }),
  execute: async ({ query }) => {
    // البحث باستخدام Exa
    const results = await exa.searchAndContents(query, { numResults: 5 });
    return { success: true, results: searchResults, count: 5 };
  },
}),

// Tool 2: توليد المقال (AI SDK 6 - Output.object)
generate_article: tool({
  description: 'Generate a structured article based on search results',
  inputSchema: z.object({
    topic: z.string(),
    searchResults: z.string(),
  }),
  execute: async ({ topic, searchResults }) => {
    // استخدام generateText مع Output.object (بدل generateObject الملغاة)
    const result = await generateText({
      model: vertex('gemini-2.5-flash'),
      output: Output.object({ schema: ArticleSchema }),
      prompt: `اكتب مقال عن: ${topic}...`,
    });
    return { success: true, article: result.output };
  },
}),

// Tool 3: توليد الصور
generate_image: tool({
  description: 'Generate an image for an article point',
  inputSchema: z.object({
    prompt: z.string(),
    pointIndex: z.number(),
    heading: z.string(),
  }),
  execute: async ({ prompt, pointIndex, heading }) => {
    // توليد صورة باستخدام Imagen 3
    return { success: true, imageData, pointIndex, heading };
  },
}),
```

### تشغيل الـ Agent (AI SDK 6)

```typescript
// استخدام ToolLoopAgent (الطريقة الجديدة في AI SDK 6)
import { ToolLoopAgent, stepCountIs } from 'ai';

const agent = new ToolLoopAgent({
  model: vertex('gemini-2.5-flash'),
  instructions: AGENT_INSTRUCTIONS, // بدل system
  tools: agentTools,
  stopWhen: stepCountIs(10), // الافتراضي في SDK 6 هو 20
});

const result = await agent.generate({
  prompt: userMessage,
});

console.log(result.text);  // الرد النهائي
console.log(result.steps); // كل الخطوات
```

---

## 🧩 المكونات (Components)

### ChatPanel.tsx - لوحة الشات

```typescript
interface ChatPanelProps {
  messages: Message[];     // قائمة الرسائل
  input: string;           // النص المدخل
  setInput: (value: string) => void;  // تغيير النص
  onSend: () => void;      // إرسال
  isLoading: boolean;      // حالة التحميل
  status: string;          // رسالة الحالة
}
```

**الوظائف:**
- عرض الرسائل السابقة
- حقل إدخال النص
- زر الإرسال
- عرض حالة التحميل

---

### ArticlePanel.tsx - لوحة المقال

```typescript
interface ArticlePanelProps {
  article: Article | null;  // المقال
  isLoading: boolean;       // حالة التحميل
  status: string;           // رسالة الحالة
}
```

**الحالات الثلاثة:**
1. **تحميل بدون مقال** → عرض Skeleton + Loader
2. **لا يوجد مقال** → رسالة "لا يوجد مقال بعد"
3. **يوجد مقال** → عرض المقال كاملاً

**المكون الفرعي ArticlePointCard:**
- يعرض نقطة واحدة مع صورتها
- يبدّل الاتجاه (نص-صورة أو صورة-نص) حسب الفهرس

---

### ChatMessage.tsx - رسالة واحدة

```typescript
interface ChatMessageProps {
  role: "user" | "assistant";  // المرسل
  content: string;              // المحتوى
}
```

- رسائل المستخدم ← يمين، خلفية وردية
- رسائل الـ AI ← يسار، خلفية شفافة

---

### AnimatedBackground.tsx - الخلفية المتحركة

```typescript
const AnimatedBackground = () => {
  return (
    <div className="fixed inset-0 overflow-hidden pointer-events-none z-0">
      {/* 6 خطوط متحركة */}
      {[...Array(6)].map((_, i) => (
        <div
          key={i}
          className="animated-stripe h-full"
          style={{
            left: `${10 + i * 15}%`,
            animationDelay: `${i * 0.8}s`,
            opacity: 0.25 + (i % 3) * 0.15,
          }}
        />
      ))}
      
      {/* كرتين ضبابيتين للتوهج */}
      <div className="absolute top-1/4 -left-32 w-96 h-96 rounded-full blur-3xl" />
      <div className="absolute bottom-1/4 -right-32 w-96 h-96 rounded-full blur-3xl" />
    </div>
  );
};
```

---

### Loader.tsx - مؤشر التحميل

```typescript
const Loader = () => {
  return (
    <div className="flex items-center justify-center gap-2 py-4">
      <div className="loader-dot" style={{ animationDelay: '0s' }} />
      <div className="loader-dot" style={{ animationDelay: '0.2s' }} />
      <div className="loader-dot" style={{ animationDelay: '0.4s' }} />
    </div>
  );
};
```

3 نقاط تنبض بتأخير مختلف

---

## 🎨 التنسيقات - globals.css

### المتغيرات الأساسية

```css
:root {
  --background: 240 20% 6%;      /* خلفية داكنة جداً */
  --foreground: 0 0% 95%;        /* نص أبيض تقريباً */
  --primary: 338 100% 60%;       /* وردي/أحمر (اللون الرئيسي) */
  --accent: 280 80% 60%;         /* بنفسجي */
  --muted: 240 15% 18%;          /* رمادي */
  --card: 240 15% 10%;           /* لون البطاقات */
  --border: 240 15% 20%;         /* لون الحدود */
}
```

### الـ Classes المخصصة

| Class | الوظيفة |
|-------|---------|
| `.glass-card` | بطاقة شفافة مع blur |
| `.glow-border` | حدود متوهجة |
| `.glow-text` | نص متوهج |
| `.animated-stripe` | الخطوط المتحركة |
| `.chat-bubble-user` | فقاعة رسالة المستخدم |
| `.chat-bubble-ai` | فقاعة رسالة الـ AI |
| `.loader-dot` | نقطة التحميل |
| `.animate-shimmer` | تأثير اللمعان |
| `.scrollbar-thin` | شريط تمرير رفيع |

### الـ Animations

```css
/* حركة الخطوط */
@keyframes stripeMove {
  0%, 100% { transform: translateY(-100%); opacity: 0; }
  50% { transform: translateY(100%); opacity: 1; }
}

/* نبض التحميل */
@keyframes loaderPulse {
  0%, 80%, 100% { transform: scale(0.6); opacity: 0.4; }
  40% { transform: scale(1); opacity: 1; }
}

/* ظهور من الأسفل */
@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

/* تأثير اللمعان */
@keyframes shimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}
```

---

## 🔄 سير العمل الكامل (Flow)

```
┌─────────────────────────────────────────────────────────────┐
│  المستخدم يكتب: "اكتب مقال عن الذكاء الاصطناعي"            │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  page.tsx → handleSend() → fetch('/api/agent')              │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  Gemini يحلل الطلب → tool: "generate_article"               │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  Exa يبحث عن "الذكاء الاصطناعي" → 5 نتائج                   │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  Gemini يكتب المقال (Output.object + ArticleSchema)         │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  Stream يرسل المقال للـ Frontend → ArticlePanel يعرضه       │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  Imagen 3 يولد 3 صور (مع delay 3 ثواني بين كل صورة)        │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  Stream يرسل كل صورة → ArticlePanel يحدث الصور تدريجياً     │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  ✅ المقال يظهر كاملاً مع الصور!                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 ملخص التقنيات المستخدمة

| التقنية | الاستخدام |
|---------|-----------|
| **Next.js 16** | الإطار الأساسي (App Router) |
| **AI SDK 6** | ToolLoopAgent + Output.object |
| **TypeScript** | الكتابة الآمنة للأنواع |
| **Tailwind CSS 4** | التنسيقات والتصميم |
| **Gemini 2.5 Flash** | توليد النصوص وتحليل الطلبات |
| **Imagen 3** | توليد الصور عالية الجودة |
| **Exa** | البحث على الإنترنت |
| **Zod** | التحقق من صحة البيانات |
| **ReadableStream** | عرض النتائج تدريجياً |
| **Lucide React** | الأيقونات |

---

## 🔑 المتغيرات البيئية المطلوبة (.env.local)

```env
EXA_API_KEY=your_exa_api_key
GOOGLE_VERTEX_PROJECT=your_project_id
GOOGLE_VERTEX_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=path_to_credentials.json
```

---

## 🚀 تشغيل المشروع

```bash
# تثبيت الاعتماديات
npm install

# تشغيل في وضع التطوير
npm run dev

# بناء للإنتاج
npm run build

# تشغيل الإنتاج
npm start
```

---

**تم إعداد هذا التوثيق بتاريخ:** يناير 2026

